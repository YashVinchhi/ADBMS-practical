# FULL OUTER JOIN Examples
###  37. Use FULL OUTER JOIN between OEHR_EMPLOYEES and OEHR_DEPARTMENTS to show all employees and all departments.
```SQL
SELECT *
FROM OEHR_EMPLOYEES E
FULL OUTER JOIN OEHR_DEPARTMENTS D ON E.DEPARTMENT_ID = D.DEPARTMENT_ID;
```
###  38. Use FULL OUTER JOIN between OEHR_CUSTOMERS and OEHR_ORDERS to show all customers and all orders.
```SQL
SELECT *
FROM OEHR_CUSTOMERS C
FULL OUTER JOIN OEHR_ORDERS O ON C.CUSTOMER_ID = O.CUSTOMER_ID;
```
###  39. Use FULL OUTER JOIN between OEHR_PRODUCT_INFORMATION and OEHR_INVENTORIES to show all products and all inventory records.
```SQL
SELECT *
FROM OEHR_PRODUCT_INFORMATION P
FULL OUTER JOIN OEHR_INVENTORIES I ON P.PRODUCT_ID = I.PRODUCT_ID;
```
###  40. Use FULL OUTER JOIN between OEHR_WAREHOUSES and OEHR_INVENTORIES to show all warehouses and all inventory records.
```SQL
SELECT *
FROM OEHR_WAREHOUSES W
FULL OUTER JOIN OEHR_INVENTORIES I ON W.WAREHOUSE_ID = I.WAREHOUSE_ID;
```
###  41. Use FULL OUTER JOIN between OEHR_EMPLOYEES and OEHR_JOB_HISTORY to show all employees and all job history records.
```SQL
SELECT *
FROM OEHR_EMPLOYEES E
FULL OUTER JOIN OEHR_JOB_HISTORY J ON E.EMPLOYEE_ID = J.EMPLOYEE_ID;
```
###  42. Use FULL OUTER JOIN between OEHR_CUSTOMERS and OEHR_COUNTRIES to show all customers and all countries.
```SQL
SELECT *
FROM OEHR_CUSTOMERS C
FULL OUTER JOIN OEHR_COUNTRIES CO ON C.COUNTRY_ID = CO.COUNTRY_ID;
```
###  43. Use FULL OUTER JOIN between OEHR_PRODUCT_INFORMATION and OEHR_ORDER_ITEMS to show all products and all order items.
```SQL
SELECT *
FROM OEHR_PRODUCT_INFORMATION P
FULL OUTER JOIN OEHR_ORDER_ITEMS OI ON P.PRODUCT_ID = OI.PRODUCT_ID;
```
###  44. Use FULL OUTER JOIN between OEHR_DEPARTMENTS and OEHR_LOCATIONS to show all departments and all locations.
```SQL
SELECT *
FROM OEHR_DEPARTMENTS D
FULL OUTER JOIN OEHR_LOCATIONS L ON D.LOCATION_ID = L.LOCATION_ID;
```
###  45. Use FULL OUTER JOIN between OEHR_ORDERS and OEHR_EMPLOYEES to show all orders and all employees.
```SQL
SELECT *
FROM OEHR_ORDERS O
FULL OUTER JOIN OEHR_EMPLOYEES E ON O.EMPLOYEE_ID = E.EMPLOYEE_ID;
```
###  46. Use FULL OUTER JOIN between OEHR_PRODUCT_INFORMATION and OEHR_PRODUCT_DESCRIPTIONS to show all products and all descriptions.
```SQL
SELECT *
FROM OEHR_PRODUCT_INFORMATION P
FULL OUTER JOIN OEHR_PRODUCT_DESCRIPTIONS PD ON P.PRODUCT_ID = PD.PRODUCT_ID;
```
###  47. Use FULL OUTER JOIN between OEHR_WAREHOUSES and OEHR_LOCATIONS to show all warehouses and all locations.
```SQL
SELECT *
FROM OEHR_WAREHOUSES W
FULL OUTER JOIN OEHR_LOCATIONS L ON W.LOCATION_ID = L.LOCATION_ID;
```
###  48. Use FULL OUTER JOIN between OEHR_EMPLOYEES and OEHR_JOBS to show all employees and all jobs.
```SQL
SELECT *
FROM OEHR_EMPLOYEES E
FULL OUTER JOIN OEHR_JOBS J ON E.JOB_ID = J.JOB_ID;
```